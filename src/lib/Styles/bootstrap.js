import { install as muiInstall } from "@material-ui/styles";

/**
 * super cool install function
 * @summary sets up everything you need for styling
 * @since 0.1.0
 */
const install = () => {
  muiInstall();
};

export default install();

export { install };
